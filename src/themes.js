import { createTheme } from "@mui/system";

export const theme = createTheme({
    palette: {
        primary: {
            main: "grey",
        },
        secondary: {
            main: "antiquewhite",
        },
    },
});